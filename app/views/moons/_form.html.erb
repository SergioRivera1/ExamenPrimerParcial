<%= form_with(model: moon, local: true) do |form| %>
  <% if moon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(moon.errors.count, "error") %> prohibited this moon from being saved:</h2>

      <ul>
      <% moon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre %>
    <%= form.text_field :name, id: :moon_name %>
  </div>

  <div class="field">
    <%= form.label :"Planeta a que pertenece" %>
    <%= form.text_field :pertenece, id: :moon_pertenece %>
  </div>

  <div class="actions">
    <%= form.submit :"Crear Luna" %>
  </div>
<% end %>
