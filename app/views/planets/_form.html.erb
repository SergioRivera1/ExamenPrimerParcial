<%= form_with(model: planet, local: true) do |form| %>
  <% if planet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planet.errors.count, "error") %> prohibited this planet from being saved:</h2>

      <ul>
      <% planet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre %>
    <%= form.text_field :name, id: :planet_name %>
  </div>

  <div class="field">
    <%= form.label :Ubicacion %>
    <%= form.text_field :ubication, id: :planet_ubication %>
  </div>

  <div class="field">
    <%= form.label :"Km de longitud" %>
    <%= form.number_field :kmlongitud, id: :planet_kmlongitud %>
  </div>

  <div class="actions">
    <%= form.submit :"Crear Planeta" %>
  </div>
<% end %>
